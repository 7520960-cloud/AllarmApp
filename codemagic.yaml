workflows:
  ios-appstore:
    name: iOS App Store Build

    integrations:
      app_store_connect: Coda3   # интеграция из Codemagic UI

    environment:
      ios_signing:
        provisioning_profiles:
          - alarmapp_appstore_profile   # твой профиль, загруженный в Codemagic

    scripts:
      - name: Build archive
        script: |
          xcodebuild \
            -project AlarmApp.xcodeproj \
            -scheme AlarmApp \
            -configuration Release \
            -archivePath $CM_BUILD_DIR/ios_app.xcarchive \
            DEVELOPMENT_TEAM=CW6VS28729 \
            PRODUCT_BUNDLE_IDENTIFIER=com.serhiitkachuk.AlarmApp \
            PROVISIONING_PROFILE_SPECIFIER=alarmapp_appstore_profile \
            archive

      - name: Export .ipa
        script: |
          xcodebuild -exportArchive \
            -archivePath $CM_BUILD_DIR/ios_app.xcarchive \
            -exportOptionsPlist exportOptions.plist \
            -exportPath $CM_BUILD_DIR

    artifacts:
      - $CM_BUILD_DIR/*.ipa
      - $CM_BUILD_DIR/ios_app.xcarchive

    publishing:
      app_store_connect:
        auth: integration   # используем интеграцию Coda3
        submit_to_testflight: true
        submit_to_app_store: false
